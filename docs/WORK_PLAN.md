# Remaining work plan

1. Install the core environment and run the smoke test.
2. Copy the prepared defense video to `data/demo.mp4`.
3. Tune only `conf`, `min_votes` and `min_hits` on that video.
4. Record real FPS, event count and failure cases.
5. Capture evidence screenshots and database records.
6. Update report/slide values with real run outputs.
7. Push the clean repository to GitHub.
8. Rehearse the distinction between detector metrics and system metrics.
